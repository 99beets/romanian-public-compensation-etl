# ---- Postgres connection (used by local Docker + ETL + dbt) ----

PGHOST=localhost
PGPORT=5432
PGDATABASE=indemnizatii
PGUSER=postgres
PGPASSWORD=postgres

# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/indemnizatii

# --- Paths / runtime ---
VENV_PATH=.venv