# Copy this file to .env.cloud and fill in real values.
# Do NOT commit real credentials.

# Cloud PostgreSQL (RDS)

PGHOST=your-rds-endpoint.amazonaws.com
PGPORT=5432
PGDATABASE=indemnizatii
PGUSER=your_db_user
PGPASSWORD=your_password

# AWS configuration
AWS_DEFAULT_REGION=eu-north-1

# S3
S3_ARTIFACTS_BUCKET=your-artifacts-bucket